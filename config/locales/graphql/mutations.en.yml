en:
  graphql:
    mutations:
      desc: The schema’s entry-point for mutations.
      common:
        fields:
          id: ID
          uuid: UUID
      user:
        add_list_item:
          desc: Add movie to a user's list
        create_list:
          desc: Create user's list
        delete_list:
          desc: Delete user's list
        remove_favorite_movie:
          desc: Remove a movie from user's Favorites list
        remove_watchlist_movie:
          desc: Remove a movie from user's Watchlist
        remove_list_item:
          desc: Remove user's list item
        sign_up:
          desc: Sign up a user
        sign_out:
          desc: Sign out a user
          fields:
            completed: will return true if the user log out
        sign_in:
          desc: Sign in a user
      auth:
        refresh_token:
          desc: Updating current access token with refresh token

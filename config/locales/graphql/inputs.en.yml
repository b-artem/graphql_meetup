en:
  graphql:
    inputs:
      delete_input:
        desc: Delete entity
        args:
          id: Entity ID
      user:
        add_list_item:
          desc: Add movie to a user's list
          args:
            list_id: List ID
            movie_id: Movie ID
        create_list:
          desc: Add a new list
          args:
            name: List name
        remove_list_item:
          desc: Remove a movie from a user's list
          args:
            list_id: List ID
            movie_id: Movie ID
        remove_favorite_movie:
          desc: Remove a movie from user's Favorites list
          args:
            movie_id: Movie ID
        add_favorite_movie:
          desc: Add a movie to user's Favorite list
          args:
            movie_id: Movie ID
        add_watchlist_movie:
          desc: Add a movie to user's Watchlist
          args:
            movie_id: Movie ID
        remove_watchlist_movie:
          desc: Remove a movie from user's Watchlist
          args:
            movie_id: Movie ID
        sign_up_input:
          desc: Sign up user via email
          args:
            first_name: User's first name
            last_name: User's last name
            email: User's account email
            password: User's account password
            password_confirmation: User's account password confirmation
        sign_in_input:
          desc: Sign in user via email
          args:
            email: User's account email
            password: User's account password
